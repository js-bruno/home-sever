# docs-vhost.nix — (removido)
#
# O vhost LAN do Quartz (docs.thisdev.space) agora é servido pelo módulo
# reverse-proxy.nix na porta 80. Este arquivo está vazio de propósito —
# mantido só como referência do que existia. Pode ser apagado.
{ ... }: { }